# lifecycle-kit: CI/CD, docs site, npm publish, OIDC trusted publisher

Full scope from user's task: structure workflows as ci.yml > release.yml >
cd.yml; dependabot + pre-commit + release-please + conventional commits;
sensible non-blocking branch protection that prevents forks from
compromising the repo; GitHub Pages + Astro docs under jonbogaty.com/lifecycle-kit;
pnpm workspace (root lib + docs/); brand docs from the hero image; SHA-pin
every action via `gh`; publish to npm as jbdevprimary first; configure npm
OIDC trusted publishing via Claude in Chrome once published.

## Queue

- [x] Move in-progress WIP (npm rescope to @jbdevprimary/lifecycle-kit) off
      local main onto feat/ci-cd-docs-pipeline; commit as logical
      conventional commits
- [x] Resolve exact SHAs via `gh api` for every action used across
      ci.yml/release.yml/cd.yml/codeql.yml (no training-data SHAs)
- [x] ci.yml: SHA-pin actions/checkout, pnpm/action-setup, actions/setup-node
- [x] Restore codeql.yml (currently deleted in working tree), SHA-pinned
- [x] release.yml: SHA-pin actions; wire npm publish auth from Doppler
      gha/ci AGENTIC_NPM_TOKEN -> gh secret NPM_TOKEN
- [x] Add .github/workflows/cd.yml: build docs/ (Astro Starlight), deploy to
      GitHub Pages via upload-pages-artifact + deploy-pages, SHA-pinned,
      ASTRO_BASE=/lifecycle-kit/ ASTRO_SITE=https://jonbogaty.com/lifecycle-kit
- [x] Enable GitHub Pages (build_type: workflow) via gh api — confirmed
      html_url resolves to http://jonbogaty.com/lifecycle-kit/
- [x] Add .github/dependabot.yml (npm root + docs/, github-actions, weekly grouped)
- [x] Add .pre-commit-config.yaml (biome check --write, trailing-whitespace,
      end-of-file-fixer, conventional-commit-msg check)
- [x] Apply baseline branch protection to main via gh api (require PR before
      merge; no required review count; block force-push/deletion; require
      conversation resolution)
- [ ] Add required status checks (verify, compatibility legs, CodeQL
      analyze) to main branch protection once first CI run establishes
      those check names on GitHub
- [x] pnpm-workspace.yaml: add packages: ["docs"]
- [x] Scaffold docs/ Astro + Starlight project (starlight-typedoc for API
      reference from src/**/index.ts per subpath, starlight-llms-txt for
      generated /llms.txt + /llms-full.txt)
- [x] Custom branding: CSS tokens from hero image palette (parchment
      #f9f5f0 bg, sage #365c4a accent, dusty rose, ochre, teal-gray),
      hero image as homepage banner, favicon derived from illustration
- [x] Root README.md: link to https://jonbogaty.com/lifecycle-kit/; audit
      all relative links/images for npm-registry-safe rendering (found +
      fixed 2 real base-path bugs: hero action links and in-body markdown
      link both needed relative paths, not root-absolute, to survive the
      /lifecycle-kit/ base — verified against full built HTML, zero
      unprefixed internal links remain)
- [x] Add AGENTS.md at repo root (build/test/verify instructions for agents)
- [x] Confirm docs build emits root-reachable llms.txt story (generated by
      starlight-llms-txt at build; document how an agent finds it)
- [x] Read through docs site as a human dev would: README -> every link;
      verified via full built-HTML href/src scan rather than a separate
      link-checker tool (none needed — scan is exhaustive and cheap)
- [x] pnpm verify green; Astro build green, no broken links
- [x] Mid-turn correction: latest tooling widest-range policy. Upgraded
      typescript 5.9.3->7.0.2, vitest 3.2.7->4.1.11, biome/attw/esbuild to
      latest; verified TS7 typechecks+builds clean against this repo first.
      vitest4's more precise branch-coverage instrumentation surfaced one
      real bug (src/assemblage/light.ts: dead `?? 0` branch after an
      already-true Number.isFinite check) - fixed with a real typeof guard,
      not a threshold change. Rest of newly-flagged branches confirmed as
      legitimate defensive/exhaustiveness code unreachable via the typed
      public API (Backbone closed union default case, Composition's
      always-present numeric fields, groupByPart's shapes-nonempty
      invariant) - left as-is, no `as any`/coverage-ignore added.
      packageManager stays the single pnpm-version source (Corepack +
      pnpm/action-setup auto-detect it); mise.toml added for LOCAL Node
      version only, mirrors .nvmrc (mise doesn't read .nvmrc by default);
      CI continues to use official actions/pnpm/setup-node only, never mise.
      engines.node stays >=22 (widest floor = matrix's tested minimum).
- [ ] Push branch, open PR as draft
- [ ] Wait CI green, promote PR to ready, merge
- [ ] Verify Pages live at jonbogaty.com/lifecycle-kit
- [ ] Verify npm publish succeeded under jbdevprimary
- [ ] Use Claude in Chrome to configure npm trusted publishing (OIDC) for
      this repo/workflow; then drop NPM_TOKEN secret + id-token-only publish
